import { KebaCasePipe } from './keba-case.pipe';

describe('KebaCasePipe', () => {
  it('create an instance', () => {
    const pipe = new KebaCasePipe();
    expect(pipe).toBeTruthy();
  });
});
